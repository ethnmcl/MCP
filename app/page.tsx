export default function Page() {
  return (
    <main>
      <h1>MCP Prototype API</h1>
      <p>Use the API routes under <code>/api</code>. This page is just here so Next builds cleanly.</p>
    </main>
  );
}
